# [เลิกใช้แล้ว] วิธีเชื่อม RSVP ในเว็บเข้ากับ Google Form/Sheet

> **หมายเหตุ**: ระบบ RSVP เปลี่ยนไปใช้ Supabase แทน Google Form แล้ว (ดู `supabase-setup.md` แทน) เพราะแก้ไขคำตอบเดิมได้จริง และมีหน้า Dashboard ให้ดูผล เอกสารนี้เก็บไว้เผื่ออยากย้อนกลับไปใช้ Google Form เท่านั้น ไม่ต้องทำตามแล้ว

ฟอร์ม RSVP บนเว็บ (`index.html`) ยังคงหน้าตาสวยงามเดิมทุกอย่าง แต่เบื้องหลังตอนกด "ส่งคำตอบ" จะแอบส่งข้อมูลไปเก็บใน Google Form ของคุณ (ซึ่งเชื่อมกับ Google Sheet โดยอัตโนมัติ) ทำตาม 5 ขั้นตอนนี้ครั้งเดียวก็ใช้งานได้จริง

## 1. สร้าง Google Form
ไปที่ https://forms.google.com แล้วสร้างฟอร์มใหม่ ตั้งคำถาม **4 ข้อ** ให้ตรงกับในเว็บ (ลำดับ/ชื่อคำถามจะเป็นอะไรก็ได้ แต่ประเภทคำถามควรตรงตามนี้):

1. ชื่อ-นามสกุล → ประเภท "ข้อความแบบสั้น" (Short answer)
2. ท่านจะเข้าร่วมงานหรือไม่ → ประเภท "หลายตัวเลือก" (Multiple choice) มี 2 ตัวเลือก
3. จำนวนผู้ร่วมงาน → ประเภท "ข้อความแบบสั้น" (Short answer)
4. ข้อความถึงคู่บ่าวสาว → ประเภท "ย่อหน้า" (Paragraph)

## 2. เชื่อมกับ Google Sheet
ในหน้าสร้างฟอร์ม กดแท็บ "การตอบกลับ" (Responses) → กดไอคอน Sheets สีเขียว → "สร้างสเปรดชีตใหม่" คำตอบทุกอันที่ส่งเข้ามาจะโผล่ในชีตนี้แบบเรียลไทม์

## 3. หาลิงก์ปลายทางสำหรับส่งข้อมูล (Form Action URL)
1. กดปุ่ม "ส่ง" (Send) มุมขวาบนของฟอร์ม
2. เลือกแท็บไอคอน `< >` (Embed HTML)
3. จะเห็นโค้ดคล้าย `<iframe src="https://docs.google.com/forms/d/e/1FAIpQLSxxxxxxxxxxxxxxxxxxxxxxxxxxxx/viewform?embedded=true" ...>`
4. คัดลอกส่วน `https://docs.google.com/forms/d/e/1FAIpQLSxxxxxxxxxxxxxxxxxxxxxxxxxxxx` แล้วเติม `/formResponse` ต่อท้าย เช่น:
   `https://docs.google.com/forms/d/e/1FAIpQLSxxxxxxxxxxxxxxxxxxxxxxxxxxxx/formResponse`
   นี่คือค่าที่จะเอาไปใส่แทน `PASTE_YOUR_FORM_ACTION_URL_HERE` ในไฟล์ `index.html`

## 4. หาเลข entry ID ของแต่ละคำถาม
1. เปิดฟอร์ม (ที่ยังไม่ได้กด Send) ในโหมดดูตัวอย่าง (กดไอคอนรูปตา มุมขวาบน)
2. คลิกขวาที่หน้าเว็บ → "ดูซอร์สหน้าเว็บ" (View Page Source) หรือกด Ctrl+U
3. กด Ctrl+F ค้นหาคำว่า `entry.` จะเจอเลขคล้าย `entry.123456789` อยู่คู่กับคำถามแต่ละข้อ (ไล่ตามลำดับคำถามที่คุณสร้างไว้)
   - วิธีที่ง่ายกว่า: กรอกคำตอบตัวอย่างในฟอร์มแล้วกด "ส่ง" 1 ครั้ง จากนั้นดูที่ URL แถบที่อยู่ หรือกดปุ่ม "แก้ไขคำตอบของคุณ" ลิงก์ที่ได้จะมีค่า `entry.XXXXXXXXX=คำตอบที่คุณกรอก` ต่อท้ายให้เห็นตรงๆ
4. จดเลข entry ID ของทั้ง 4 คำถามให้ตรงกับลำดับ: ชื่อ / เข้าร่วมหรือไม่ / จำนวนผู้ร่วมงาน / ข้อความ

## 5. ใส่ค่าในไฟล์ index.html แล้วทดสอบ
เปิด `index.html` หาบล็อกโค้ดนี้ใกล้ต้นไฟล์ `<script>` (คอมเมนต์ "ตั้งค่าการส่ง RSVP"):

```js
var GOOGLE_FORM_ACTION = "PASTE_YOUR_FORM_ACTION_URL_HERE";
var GOOGLE_FORM_ENTRIES = {
  name: "PASTE_ENTRY_ID_FOR_NAME",
  attend: "PASTE_ENTRY_ID_FOR_ATTEND",
  guests: "PASTE_ENTRY_ID_FOR_GUESTS",
  message: "PASTE_ENTRY_ID_FOR_MESSAGE"
};
```

แทนที่ค่าทั้ง 5 ตัวด้วยของจริงจากข้อ 3-4 (entry ID ใส่แค่ตัวเลข ไม่ต้องมีคำว่า `entry.` ซ้ำ เพราะโค้ดใส่ให้เองผ่านชื่อ key เช่น `"entry.123456789"` ใส่ทั้งสตริงแบบนี้ไปเลยก็ได้)

จากนั้น **เปิดเว็บจริงแล้วลองกรอก RSVP ส่งดู 1 ครั้ง** แล้วไปเช็ค Google Sheet ว่ามีแถวใหม่ขึ้นมาถูกต้องหรือไม่ — ขั้นตอนนี้สำคัญมาก เพราะวิธีส่งข้อมูลแบบนี้ (`no-cors`) จะไม่มีข้อความ error ใดๆ ขึ้นบนเว็บแม้จะตั้งค่าผิด ต้องเช็คที่ปลายทาง (Sheet) เท่านั้น

## หมายเหตุ
- ถ้ายังไม่ตั้งค่า (ยังเป็น `PASTE_...` อยู่) เว็บจะยังทำงานปกติ กดส่งแล้วขึ้นข้อความขอบคุณเหมือนเดิม แต่ข้อมูลจะไม่ถูกบันทึกไปที่ไหน (จะมี warning ขึ้นใน console เตือนไว้)
- อยากดูรายชื่อแขกที่ตอบรับ ก็เปิด Google Sheet ที่เชื่อมไว้ได้ตลอดเวลา ไม่ต้องพึ่งเว็บ
